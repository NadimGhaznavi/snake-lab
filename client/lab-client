#!/usr/bin/env bash
# Installed SnakeLab administrative client launcher.

set -Eeuo pipefail

readonly INSTALL_DIR="/opt/snake-lab"

cd -- "${INSTALL_DIR}/app"
exec "${INSTALL_DIR}/venv/bin/python" -m snake_lab.client "$@"

