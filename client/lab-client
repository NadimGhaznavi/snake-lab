#!/usr/bin/env bash
# Installed SnakeLab Textual client launcher.

set -Eeuo pipefail

readonly INSTALL_DIR="/opt/snake-lab"

PYTHONPATH="${INSTALL_DIR}/app" \
    exec "${INSTALL_DIR}/venv/bin/python" -m snake_lab.client "$@"
